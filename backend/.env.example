# =============================================================================
# SERV.O v10.4 - Configurazione Ambiente
# =============================================================================
# Copia questo file in .env: cp .env.example .env
# =============================================================================

# Database PostgreSQL
PG_HOST=127.0.0.1
PG_PORT=5432
PG_DATABASE=servo
PG_USER=servo_user
PG_PASSWORD=servo_pwd

# Tipo database: postgresql o sqlite
DB_TYPE=postgresql

# SQLite (solo per migrazione/fallback)
DB_PATH=extractor_to.db

# Directory
UPLOAD_DIR=uploads
OUTPUT_DIR=outputs

# Backend URL (per generazione link)
BACKEND_URL=http://localhost:8000

# =============================================================================
# CREDENZIALI DA CONFIGURARE MANUALMENTE (non committare!)
# =============================================================================

# JWT Secret (genera con: openssl rand -hex 32)
# JWT_SECRET=your_secret_here

# Gmail Monitor (opzionale)
# GMAIL_USER=your_email@gmail.com
# GMAIL_APP_PASSWORD=your_app_password

# =============================================================================
# NOTE PER CODESPACES
# =============================================================================
# In Codespaces devi avere un database PostgreSQL accessibile.
# Opzioni:
# 1. Usa un servizio cloud (ElephantSQL, Supabase, Neon, etc.)
# 2. Configura PostgreSQL in un devcontainer
# 3. Usa SQLite cambiando DB_TYPE=sqlite (funzionalita limitata)
# =============================================================================
